// regexp = new RegExp("шаблон", "флаги");
// regexp = /шаблон/; // без флагов
// regexp = /шаблон/gmi; // с флагами gmi (будут описаны далее)

let str = 'Грунт - горная порода';
let f = str.match(/грунт/i);
console.log(f);